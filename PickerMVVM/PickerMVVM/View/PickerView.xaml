<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PickerMVVM.View.PickerView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Elejir Ciudad" FontSize="Medium" />
            <Picker Title="Selecione"
                    ItemsSource="{Binding ListaCiudades}"
                    ItemDisplayBinding="{Binding valor}"
                    SelectedItem="{Binding SelecionarCiudad}"/>
            <Label Text="{Binding miciudad } " TextColor="Teal"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>